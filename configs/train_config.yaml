block_size: 256
vocab_size: 6000
n_layer: 6
n_head: 8
n_embd: 384
batch_size: 16
learning_rate: 0.001
epochs: 5          # Keep low for CPU — each epoch over 31k files takes ~hours
max_steps: 5000    # Hard cap: stop after this many steps regardless of epochs
dropout: 0.1
